// lib/site.ts
// Canonical public origin (deploy configuration, not content — D-15). This
// is chun-ju.irilia.app, NOT iridiumtao.github.io (which merely 301s to it).
// Needed because OG tags and canonical links require absolute URLs.
export const SITE_ORIGIN: string = "https://chun-ju.irilia.app";
